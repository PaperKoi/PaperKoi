# 工作流名称：生成贪吃蛇动画与 3D 城市贡献图
name: 生成贪吃蛇与 3D 城市贡献图

on:
  # 定时触发：每天 UTC 00:00（北京时间 08:00）自动执行
  schedule:
    - cron: "0 0 * * *"
  # 手动触发：支持在 Actions 页面手动点击运行
  workflow_dispatch:
  # 推送触发：当 main 分支有更新时触发构建
  push:
    branches:
      - main

jobs:
  generate:
    name: 构建并部署贡献图资产
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: 检出仓库代码
        uses: actions/checkout@v4

      # 第一步：拉取贡献数据并生成贪吃蛇动画
      - name: 生成贪吃蛇动画 SVG 文件
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: Oppenheymu
          # color_dots 共 5 个颜色，依次对应：0 次贡献、以及从低到高的 4 个贡献档位。
          # 这里把最低的可视档整体加深/提亮，避免某天贡献爆量后其余格子显得过于暗淡。
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#24292e&color_dots=#ebedf0,#40c463,#2ea043,#1f883d,#0e4429
            dist/github-contribution-grid-snake-dark.svg?color_snake=#e6edf3&color_dots=#161b22,#1f6f34,#2ea043,#3fb950,#56d364
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 第二步：生成 3D 城市立体贡献图
      # 使用自定义配置：北半球四季配色 + 强制静态（关闭 growingAnimation）+ 明暗双版本
      - name: 生成 3D 城市立体贡献图
        uses: yoshi389111/github-profile-3d-contrib@0.7.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: Oppenheymu
          SETTING_JSON: .github/assets/profile-3d-contrib.json

      # 第三步：将生成的 3D 资源合并移动到 dist 目录中，便于统一发布
      - name: 整合所有 SVG 文件至 dist 目录
        run: |
          mkdir -p dist/profile-3d-contrib
          cp -r profile-3d-contrib/* dist/profile-3d-contrib/

      # 第四步：将 dist 目录的所有文件推送到 output 分支
      - name: 发布资源文件到 output 分支
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
